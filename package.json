{
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "name": "ttime",
  "version": "1.0.0",
  "description": "Tee Time is a web app that manages Course Tee times and checks in each party, and makes an additional\r check to the membership database for status.",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm start",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run start\" \"npm run client\""
  },
  "author": "",
  "license": "ISC"
}
